# AquaOS — Monitor Configuration
# Auto-detect all connected monitors, preferred resolution, auto position, 1× scale
# For named monitors, use: monitor = <name>, <resolution>@<hz>, <position>, <scale>
# Example: monitor = DP-1, 1920x1080@60, 0x0, 1.0

monitor = , preferred, auto, 1.0
